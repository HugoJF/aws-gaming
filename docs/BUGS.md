# BUGS

- [x] health-check popover is now too small with "Not Configured" (assume DNS), and its being clipped. Not Configured is also very vague.
- [x] the ping sidecart for the hello world example is not working, all requests are pending (also, add a timeout for requests).
- [x] `GET /api/admin/servers` returns 404.
