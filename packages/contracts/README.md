# @aws-gaming/contracts

Shared domain models for templates, instances, power state, and access tokens.

## Source

- `packages/contracts/src/index.ts`

## Commands

```bash
bun run --cwd packages/contracts build
bun run --cwd packages/contracts typecheck
```
