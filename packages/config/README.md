# @aws-gaming/config

Environment variable helpers (`getRequiredEnv`, `getEnv`).

## Source

- `packages/config/src/index.ts`

## Commands

```bash
bun run --cwd packages/config build
bun run --cwd packages/config typecheck
```
